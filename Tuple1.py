#A tuple is an ordered, immutable (unchangeable) collection. Ideal for fixed data.
point = (4, 5)

# Accessing
print(point[0])  # 4

# Unpacking
x, y = point
print(x, y)

# Tuple of different types
info = ("Alice", 22, True)
